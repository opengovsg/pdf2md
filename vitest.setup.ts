import { expect, vi } from 'vitest'
import path from 'path'

// Set up module aliases for testing
const modulesPath = path.resolve(__dirname, 'dist')

// Mock modules if needed
// vi.mock('...', () => { ... })

// Mock global objects if needed
// global.someGlobalObject = ...

// Setup any globals or expectations extensions here
// If you need custom matchers, you can add them here 
